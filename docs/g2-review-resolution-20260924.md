# 2026-09-24 拷打报告核对与处理

范围：本机当前工作区，保留原有未提交修改；未启动机器人、未推送远端。审查报告不是本机当前版本的完整镜像，以下按实际代码核对。

| 报告项 | 本轮结果 | 剩余边界 |
|---|---|---|
| 1. 子模块不可复现 | 本机确有锁定的 `d1c641d`；远端 ls-remote 只公布 `bd4b135` main。原报告提到的优化器生成器/连续动作 discrete_actor/cuda:0 初始化问题在本机版本已修；保留既有子模块改动 | 没有擅自切回旧 main 或推送别人的仓库。当前修复版尚需发布至可获取仓库并做干净克隆，不能标为解决 |
| 2. Replay/checkpoint 容量 | 现场和只读模板改为 2048/1024；checkpoint 改存有效槽位，验证恢复与去重 | 运行时图像仍 float32、约 2.25 GiB；模型/保存峰值另算。未做现场长时存储验收或 uint8 改造 |
| 3. 额外在线 Actor BC | 正式 Learner 在线主循环删除 actor_bc，只更新 Critic/Actor/λ | 其他离线工具仍可显式调用 BC；不把小 CNN 配方称为严格全文复现 |
| 4. β 就绪条件 | 默认先单独预训练 500 步；无人工数据不更新 Actor/λ；每新增 50 条人工样本再更新 β 50 步；计数/loss 保存恢复。示范 CLI 导入后先预训练再启动服务 | 500 是配置的优化步数，不是现场训练有效证明。少量样本可有放回采样，数据质量仍待确认 |
| 公式差异 | 本机也存在未修的 L2 范数/0.2；本轮改平方 L2/0.1，现场及模板 λ 学习率改 3e-6 | 保留论文自适应项的分母，不采纳旧报告对分母的错误批评 |
| 5. 前驱与命令原点 | Gym 把实际前驱传入后端，目标基于该前驱位姿；新读数只验证当前反馈。发送前重新检查前驱年龄，不推进/回退源进度；过期则丢弃动作并停止 | 不自动重复发送或重新推理。实测跟踪响应仍由后继表示，不把指令当实际位移 |
| 6. Y/F 归因 | 暂存最后一条非终止样本；下一轮开始、推理后及后端 submit 前检查 Y/F。命中则取消尚未发送的新动作，恢复上一已完成 token 并结算它；记录标签读取时间和 transition ID | submit 前最后轮询是软件归因边界，之后收到的按键随新动作后继结算；不是物理按键硬件时间戳或硬实时保证。异常可丢弃尚未提交的最后样本；现场响应时间仍待核验 |
| 7. 实际频率 | 明确 10 Hz 是名义设定；step 日志增加发送/后继读取完成时刻，并保留推理耗时和完整周期 | 没有实机周期分布，不宣称已实现稳定 10 Hz，也未擅自改变折扣 |
| 8. 视觉输入 | 起终点双相机 ROI/128×128 检查此前已完成，不再沿用“还在全帧”的结论 | 中间遮挡、约 5 cm 扰动、精细方向可辨和真实学习有效性仍未验证 |
| 9. 代码身份 | manifest/checkpoint 添加主仓库/子模块 SHA、实际源码摘要、策略展开配置、Python/依赖版本；恢复与冻结评估不匹配即拒绝 | 旧 checkpoint 需要明确迁移；在线进程间协议仍以 run/config 标识为主，不能视为远程代码证明 |

算法依据：[SiLRI 论文 IV-C/IV-D](https://arxiv.org/html/2512.24288v1)。选择正文“新增人工样本”作为 β 更新计数，默认每 50 条做 50 次更新；不按每个梯度轮次额外训练 Actor BC。

## 验证与进度判断

- 本轮相关离线检查覆盖平方距离公式、β 门槛/计数/恢复、有效槽位 checkpoint、前驱位姿不同的执行原点、过期前驱拒绝、Y/F 在后继之后到达、运动装配与新鲜度，以及自动复位回归；一次合并运行 449 项通过（多数为已有参数化输入校验，不是 449 次真机测试）。
- 后续小范围校验另见终端结果；无真机动作、无在线训练、无自主成功率数据。
- 主流程模块多数已经实现，不需要推倒重搭。但“代码存在”不等于“正式真机训练已跑通”。现在进入关键软件修正后的现场集成阶段，而不是只剩调超参数。
- 下一步：完成可获取的算法版本发布；现场确认剩余动作/停止项并跑纯人工 demo 与自动复位，核对实际周期、标签和 ROI，再做受监督的 Actor–Learner 真机闭环。未解决的发布/时序/现场项不可从待办中删除。
