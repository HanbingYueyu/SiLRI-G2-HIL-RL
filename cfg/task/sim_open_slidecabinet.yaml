task_name: sim_open_slidecabinet

lerobot:
  cameras: [front, wrist]
  state_dim: 8
  action_dim: 6
  fps: 10
  num_discrete_actions: 2
  expert_state_dim: 8
  expert_cameras: []
  expert_env_state_dim: 3


dataset: null
gripper_penalty: -0.05
close_gripper: true
fix_gripper: false
max_episode_length: 50
max_train_time: 24000
evaluation_interval: 200
evaluation_episode: 50
max_step: 5000
max_intervention_steps_per_episode: 5
save_checkpoint: true
resume_model: false



env_id: "gym_hil/PandaOpenSlideCabinetGamepad-v0"

use_gripper: true
use_gamepad: false
controller_config_path: "../../../gym-hil/gym_hil/PS5_config.json"
use_gripper_penalty: true
randomize_cabinet_yaw: true
cabinet_yaw_range_deg: 20.0
initial_door_range: None
action_noise_duration: 5
quantile_level: 0.7
intervention_signal_source: human

expert:
    load_path:  ../../rl_envs_sim/expert/sim_open_slidecabinet/pretrained_model/
    policy_type: sac_wo_img
    device: ${device}

time_penalty: 0.0
intervention_steps: 5

env_cfg:
    id: ${env_id}    
    seed: ${seed}  
    headless: ${headless}
    image_obs: true
    gripper_penalty: ${gripper_penalty}
    reward_type: ${reward_type}
    expert_cfg: null
    max_episode_steps: ${max_episode_length}
    use_inputs_control: ${use_inputs_control}
    use_gamepad: ${use_gamepad}
    controller_config_path: ${controller_config_path}
    hz: 10
    time_penalty: ${time_penalty}
    randomize_cabinet_yaw: ${randomize_cabinet_yaw}
    cabinet_yaw_range_deg: ${cabinet_yaw_range_deg}
    use_expert_control: ${use_expert_control}
    max_intervention_steps_per_episode: ${max_intervention_steps_per_episode}
    initial_door_range: ${initial_door_range}
    action_noise_duration: ${action_noise_duration}
    intervention_signal_source: ${intervention_signal_source}
    reset_delay_seconds: ${reset_delay_seconds}
    max_step: ${max_step}
    deterministic_eval: ${deterministic_eval}
    intervention_steps: ${intervention_steps}
